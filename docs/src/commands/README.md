# Commands

Crucible exposes four primary command groups:

- `crucible review`
- `crucible config`
- `crucible hook`
- `crucible session`

Use `crucible --help` or any subcommand's `--help` flag for the full flag surface.
